# 老谭 AI 工作台版本记录

版本号：v2026.05.27
日期：2026-05-27
线上 Worker 版本 ID：2128d1c2-daab-484e-8514-09d9ce12c455
正式域名：https://ai.dazhuanguwen.com/

最新发布：2026-05-27 13:30 左右
最新线上 Worker 版本 ID：95210eb1-b877-4cb9-a4d6-58c9bc5da43e
视觉精修发布：2026-05-27
视觉精修线上 Worker 版本 ID：5495b831-fda8-4406-a5cc-406b573050d5
官网与工作台统一重构发布：2026-05-27
官网与工作台统一重构线上 Worker 版本 ID：d114e614-a90e-4d82-b871-a0f1693826e0
小红书标题封面成组生成发布：2026-05-27
小红书标题封面线上 Worker 版本 ID：05e40390-0ba2-4c68-a8ba-084f50c4602a
小红书标题封面卡片展示线上 Worker 版本 ID：581d7d81-c9bd-485a-b9aa-b1f2300e42cd
工作台顶部文案更新线上 Worker 版本 ID：21fec2b5-d8ca-45f0-8df8-6ee058d79b32
空对标保护逻辑线上 Worker 版本 ID：f9a7ed21-a351-4cb6-955a-b25d1878b469

## 本次版本定位

这是老谭 AI 工作台当前可用基准版本。后续修改、优化和扩展都以这个版本为基础继续改，不再回到旧的混乱入口结构。

## 正式入口

- AI 工作台首页：https://ai.dazhuanguwen.com/workspace/
- 小红书工作台：https://ai.dazhuanguwen.com/tools/xhs/
- 话术工作台：https://ai.dazhuanguwen.com/tools/scripts/
- 项目驾驶舱：https://ai.dazhuanguwen.com/dashboard/
- 官网首页：https://ai.dazhuanguwen.com/

## 已完成内容

### 0.1 2026-05-27 追加更新：官网与工作台统一重构

- 官网首页 `/` 重构为“长沙装修签约前独立顾问”服务页，保留服务价格、流程、案例、FAQ 和企业微信弹窗。
- 官网视觉统一到小红书工作台同款白色工具感：浅灰背景、蓝绿渐变按钮、轻玻璃卡片、细网格背景，不再使用粉紫橙营销风。
- 工作台首页 `/workspace/` 简化为三个主入口：进入官网、写小红书、资料库。
- 首页小红书卡片不再放账号按钮、动作按钮、流程演示和资料小按钮，真正作为“进入小红书工作台”的入口。
- 资料库常用入口保留在第二层；旧工具继续折叠保留。

### 0.2 2026-05-27 追加更新：小红书标题封面成组生成

- 小红书生成结果新增 `titleCoverPairs` 标准字段，固定输出 5 组一一对应的“标题 + 封面”方案。
- 取消前端“标题库 / 封面脚本”分离表达，改为“标题封面 / 图文脚本”。
- 服务端提示词要求标题和封面必须成套生成，不再让模型先给标题库、再给封面库。
- 服务端标准化兼容旧字段：如果模型返回旧 `titles/script`，自动合并整理为 5 组标题封面方案。
- 前端展示兼容旧字段和本地兜底结果，接口异常时也返回成组标题封面。
- 已发布 Cloudflare 正式版本 `05e40390-0ba2-4c68-a8ba-084f50c4602a`。

### 0.3 2026-05-27 追加更新：小红书标题封面卡片展示

- “标题封面”结果页从纯文本升级为 5 张方案卡，方便快速选择要发的标题封面组合。
- 每张卡展示标题、封面大字、封面小字、画面建议、用图建议和匹配逻辑。
- 复制当前页时仍复制干净纯文本，不复制卡片 HTML。
- 草稿保存与恢复兼容卡片展示，刷新后不会丢掉可读结构。
- 已发布 Cloudflare 正式版本 `581d7d81-c9bd-485a-b9aa-b1f2300e42cd`。

### 0.4 2026-05-27 追加更新：工作台品牌文案调整

- 工作台首页顶部删除“小标签”。
- 主标题改为 `老谭 · One Person Company`。
- 副标题改为 `拥抱原生 AI Agent 工作流，以专业为壁垒，沉淀数据资产，提升系统级能力`。
- 右侧卡片改为 `2026.5 版本`。
- 已发布 Cloudflare 正式版本 `21fec2b5-d8ca-45f0-8df8-6ee058d79b32`。

### 0.5 2026-05-27 追加更新：空对标保护

- 小红书对标区新增 `清空对标` 按钮，可一键清空对标标题、正文、拆解结果和对标截图。
- 修复“没有对标文案时，旧拆解结果仍可能被模型读取”的隐患。
- 现在只有对标标题、对标正文或对标截图存在时，才会把对标拆解内容传给 GPT。
- 灰色 placeholder 提示字不会被读取；输入框真实 value 才会被读取。
- 已发布 Cloudflare 正式版本 `f9a7ed21-a351-4cb6-955a-b25d1878b469`。

### 0. 2026-05-27 追加更新：小红书识图链路和视觉升级

- 小红书“我的图片素材”默认改为 GPT-5.5 直接读图生成。
- Qwen-VL 只作为 GPT-5.5 直读图片失败后的备用 OCR/摘要链路。
- 顶部状态明确显示：识图 GPT-5.5 直读 / 备用摘要 / 无图片。
- 小红书工作台视觉升级为更有质感的白色玻璃工作台：蓝绿金辅助色、柔和光影、细网格背景、三列 16:9 布局不变。
- 视觉方向确定为“白色专业工具骨架 + 少量玻璃光影”：三列顶部用蓝/绿/金色条区分对标、品牌素材、生成区，主按钮使用蓝绿渐变，输入区和卡片阴影更克制。
- 取消对标区无效的小红书链接框，保留对标正文粘贴和对标截图 OCR。
- 保留对标图只用于拆解辅助，不会被当成我的案例素材。

### 1. 取消所有网页登录密码

- 取消 Worker 对 `/tools/xhs/` 和 `/tools/scripts/` 的密码拦截。
- `/tools/xhs-login` 改为自动跳转 `/tools/xhs/`。
- 删除 `/workspace/` 首页的静态密码层。
- 删除 `/dashboard/` 驾驶舱的静态密码层和锁定按钮。
- 保留云端 API 密钥为 Cloudflare Secret，不写入项目文件。

### 2. 工作台首页重构

- 首页主结构改为三大入口：进入官网、写小红书、资料库。
- 旧 11 个模块统一折叠为备用入口。
- 删除“今日焦点手填框”这类需要老板自己当调度员的设计。
- 小红书入口加强为账号选择、常用动作和写作路径。

### 3. 小红书工作台重构

- 桌面端改为 16:9 工作台布局，减少上下滚动。
- 三列结构：对标区、我的素材区、生成区。
- 对标文案支持自动识别首行为标题。
- 对标截图用于拆解小红书截图结构。
- 我的图片素材重新接入生成逻辑。
- 去掉无用图片页数、图片数量限制等干扰项。
- 增加模型状态显示，默认使用 GPT-5.5，高推理强度。
- 修复生成结果空行丢失问题。
- 增加接口失败时的后端兜底结果，减少直接显示“接口异常”。

### 4. 品牌知识库与云端对标库

- 老谭个人 IP 改名为“老谭装修避坑”。
- 加载完整老谭装修避坑品牌知识库。
- Cloudflare KV 接入小红书云端对标库，三台电脑可共用。
- `wrangler.toml` 增加 `XHS_BENCHMARKS` KV 绑定。

## 已自检

- `node --check worker.js` 通过。
- `public/tools/xhs/index.html` 内联脚本解析通过。
- `public/index.html`、`public/workspace/index.html`、`public/script.js` 脚本解析通过。
- 本地 `/`、`/workspace/`、`/tools/xhs/?mode=new` 路由均返回 HTTP 200。
- Chrome 本地视觉检查通过：官网和工作台桌面首屏无明显重叠，工作台第一屏只显示三个主入口。
- 线上页面源检查通过：官网已更新为“签合同前，先让老谭帮你看一眼”，工作台已更新为“只留三个入口，打开就开工”。
- 线上页面源检查通过：新视觉 CSS 已上线，且不再包含“小红书链接”输入框。
- Chrome 视觉检查通过：小红书三列在 16:9 桌面视口正常显示，无明显重叠。
- 线上 `/workspace/` 直接显示工作台，无密码页。
- 线上 `/dashboard/` 直接显示驾驶舱，无密码页。
- 线上 `/tools/xhs/` 直接显示小红书工作台，无密码页。
- 线上 `/tools/scripts/` 直接显示话术工作台，无密码页。
- `/tools/xhs-login` 返回 302，并跳转到 `/tools/xhs/`。
- `/api/xhs-generate` 返回 `ok:true`，模型为 `gpt-5.5`，推理强度为 `high`。
- 线上 `/tools/xhs/?mode=new` 已显示“标题封面 / 图文脚本”，不再显示“标题库 / 封面脚本”标签。
- 线上生成测试通过：`titleCoverPairs` 返回 5 组，`titles` 展示文本包含“封面大字”，最终正文正常返回。
- 线上 `/tools/xhs/?mode=new` 已包含标题封面卡片展示代码 `title-cover-card`。
- 线上 `/tools/xhs/?mode=new` 已包含 `清空对标` 按钮和 `hasBenchmarkInput` 空对标保护逻辑。
- 线上 `/workspace/` 已显示 `老谭 · One Person Company` 和 `2026.5 版本`。
- 线上无图生成测试通过：HTTP 200，`gpt-5.5`，`reasoning: low`，约 27 秒返回。
- 线上带图生成测试通过：HTTP 200，`directImagesAttached:true`，`visionMode:gpt-5.5-direct`，约 39 秒返回，未启用 Qwen-VL。
- `/api/xhs-benchmarks` 返回 `ok:true`，来源为 `cloudflare-kv`。
- `/api/scripts-library` 返回 `ok:true`。

## 后续修改原则

- 后续所有功能改动都从 `v2026.05.27` 继续。
- 不再恢复网页登录密码。
- 不把 API key 写入代码或文档。
- 小红书工作台继续按“高效、可用、可升级的大状 AI 工作台”方向优化。
- 每次改完必须先自检，再汇报。

## 2026-05-28 追加更新：客户线上转化系统

- 接管桌面备份 `项目备份-客户线上转化系统-20260528-160328` 中的半成品功能，没有整包覆盖当前仓库，避免冲掉小红书和工作台现有修复。
- 工作台新增主入口「客户线上转化系统」，正式地址为 `/tools/scripts/#b2b`。
- 话术工具页升级为「客户线上转化系统」，新增首个板块「B端有效接触」。
- 新增四品牌有效接触规则库：`8K设计`、`玖玖精工 / 玖玖半包`、`玖玖精装`、`宅师傅半包装修`。
- 新系统支持按品牌、微信阶段、客户级别、客户顾虑筛选，输出主动作、邀约话术、物料动作和拒绝处理。
- 新增静态规则文件 `public/tools/scripts/b2b-data.js`，并保持 `tools/scripts/b2b-data.js` 镜像同步。
- Worker 新增 `/api/scripts-b2b-library`，把 B 端有效接触规则并入话术库能力。
- 已删除另一个机器人重新加回来的 `/tools/scripts-auth` 口令页和 `SCRIPTS_ACCESS_CODE` 拦截逻辑，继续保持所有网页免密码访问。
- 已发布 Cloudflare 正式版本 `e0bad4d2-3597-46d9-ab69-76f03b341ae1`。

## 2026-05-28 追加更新：客户线上转化系统 V2

- 客户线上转化系统首页改为四品牌入口：`8K设计`、`玖玖精工 / 玖玖半包`、`玖玖精装`、`宅师傅半包装修`。
- 点击品牌后进入该品牌的“客户推进台”，主流程变为：选品牌 → 选/建客户 → 判断客户 → 生成/复制话术 → 保存结果。
- 原“大而全”的规则库、话术库、知识补充、成长复盘降级为二级折叠工具，不再占第一屏主视线。
- 新增 Cloudflare KV 绑定 `SCRIPTS_CLIENTS`，用于三台电脑同步客户推进记录。
- 新增 `/api/scripts-clients`，支持客户记录读取、创建/更新、保存结果、删除。
- 客户记录字段包含品牌、称呼、来源、楼盘、面积、预算、阶段、级别、顾虑、推荐动作、客户原话、最近回复、下次跟进、结果记录。
- 已发布 Cloudflare 正式版本 `0fcaa086-19da-4195-a9f0-6128b41317d3`。

## 2026-05-28 追加更新：8K客户推进动作

- 8K设计的主推进动作由「到公司」调整为「到公司 / 看完工」。
- 8K的客户判断、轻邀约话术和完整规则库同步加入「看完工现场」作为落地验证路径。
- 客户结果记录新增「已约看完工现场」，方便后续统计 8K 完工现场邀约。
