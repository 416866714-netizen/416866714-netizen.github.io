# 老谭官网固定更新流程

更新时间：2026-05-16

适用网站：https://dazhuanguwen.com/

## 固定项目

- Cloudflare Worker 正式项目：`curly-frog-9f44`
- 线上域名：`https://dazhuanguwen.com/`
- GitHub 仓库：`https://github.com/416866714-netizen/416866714-netizen.github.io.git`
- 不要新建 Cloudflare 项目。
- 不要用旧的三文件上传包当正式部署源。

## 本地真实仓库

```text
/Users/tanzhimin/Desktop/AI网页开发工作文件-整合包/大壮工作总控台-整合版/01-官网代码-GitHub仓库
```

正式静态资源目录：

```text
/Users/tanzhimin/Desktop/AI网页开发工作文件-整合包/大壮工作总控台-整合版/01-官网代码-GitHub仓库/public
```

Cloudflare 配置文件：

```text
/Users/tanzhimin/Desktop/AI网页开发工作文件-整合包/大壮工作总控台-整合版/01-官网代码-GitHub仓库/wrangler.toml
```

`wrangler.toml` 中 `directory = "./public"`，所以线上部署读取 `public`，不是旧根目录 `dashboard/`。

## 关键页面路径

```text
首页：
public/index.html

内部工作台：
public/dashboard/index.html

小红书内容工具：
public/tools/xhs/index.html

话术自动化工具：
public/tools/scripts/index.html

后端 Worker：
worker.js
```

## 固定部署命令

```bash
cd "/Users/tanzhimin/Desktop/AI网页开发工作文件-整合包/大壮工作总控台-整合版/01-官网代码-GitHub仓库"
npx wrangler deploy
```

如果 Wrangler 要求登录，走浏览器 OAuth 授权。2026-05-16 实测 Safari 已有 Cloudflare 登录态，可授权 Wrangler。

## 线上验证

```bash
curl -L -s "https://dazhuanguwen.com/dashboard/?v=版本号" | rg "老谭 AI 工作台|任务启动器"
curl -L -s "https://dazhuanguwen.com/api/xhs-generate"
curl -L -s "https://dazhuanguwen.com/api/scripts-reply"
```

预期：

- `/api/xhs-generate` 返回 `ok: true`、`provider: openai-only`、`model: gpt-5.5`
- `/api/scripts-reply` 返回 `ok: true`、`model: gpt-5.5`

## GitHub 注意事项

2026-05-16 普通 `git push` 曾因 GitHub HTTPS 通道 `Empty reply from server` 卡住；当时已用 GitHub API 更新远端。

后续规则：

1. 先确认远端 `main` 最新状态，再改。
2. 不要从本地旧 `main` 直接推送。
3. 不要强推。
4. 不要覆盖线上已有的 `public/workspace/`、`public/tools/`、`worker.js` 和 `wrangler.toml`。

## 每次交付前必查

- C 端价格固定：`99 / 199 / 399 / 498`。
- 不再出现 `299`、`1299`。
- 页面不能出现重复价格，例如“¥99 99元”“¥199 199元”。
- 手机端必须检查首屏标题、服务卡片、价格、按钮是否重叠。
- 工具页必须检查真实接口，不允许假接入、假结果。
- GPT Key、Cloudflare Token、OpenAI Key 不得写入前端、Markdown 或 GitHub。
